export const isAdmin = (role) => role?.toLowerCase() === "admin";
export const isEditor = (role) => role?.toLowerCase() === "editor";
export const isViewer = (role) => role?.toLowerCase() === "viewer";
