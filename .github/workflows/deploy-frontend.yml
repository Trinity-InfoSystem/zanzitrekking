name: Deploy Frontend to cPanel

# This workflow triggers ONLY when frontend files change
# It will NOT trigger if only backend files are changed
on:
  push:
    branches:
      - main
    paths:
      - 'zanzitrekking-frontend/**'
      - '.github/workflows/deploy-frontend.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./zanzitrekking-frontend
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: zanzitrekking-frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Verify artifact
        run: |
          if [ ! -d "dist" ]; then
            echo "❌ ERROR: Missing build artifact!"
            echo "Dashboard build failed. Please check the build logs."
            exit 1
          fi
          echo "✅ App artifact built successfully"
          echo "📁 Contents of artifact:"
          ls -a dist | head -20
          echo ""
          echo "📊 Total files: $(find dist -type f | wc -l)"

      - name: Install SSH client
        run: |
          apt-get update
          apt-get install -y openssh-client rsync

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.CPANEL_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p ${{ secrets.CPANEL_SSH_PORT || 22 }} ${{ secrets.CPANEL_SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Rsync files
        run: |
          rsync -avz --delete \
            -e "ssh -p ${{ secrets.CPANEL_SSH_PORT || 22 }}" \
            dist/ \
            ${{ secrets.CPANEL_SSH_USERNAME }}@${{ secrets.CPANEL_SSH_HOST }}:${{ secrets.CPANEL_FRONTEND_DEPLOYMENT_DIR }}

      - name: Deployment Notification
        if: success()
        run: |
          echo "✅ Frontend deployed successfully"
