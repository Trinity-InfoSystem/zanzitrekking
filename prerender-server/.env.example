# Optional — 64-bit Chrome on Windows is auto-detected if this is unset.
# Must be the full path to chrome.exe (not the folder).
# CHROME_PATH=C:/Program Files/Google/Chrome/Application/chrome.exe

PORT=3000
